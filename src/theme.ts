export interface IThemeInterface {
  blackColor: string;
  blueColor: string;
  greyColor: string;
  whiteColor: string;
}

const theme = {
  blackColor: "#222831",
  blueColor: "#00adb5",
  greyColor: "#393e46",
  whiteColor: "#eeeeee"
};

export default theme;
